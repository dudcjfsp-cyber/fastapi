# 📋 인텔 AI 양성과정 실습 프로젝트 - Task 현황

> 최종 업데이트: 2026-02-12

---

## 1. 백엔드 (FastAPI)

### 1.1 프로젝트 기반 구조
- [x] FastAPI 앱 초기화 (`main.py`)
- [x] CORS 미들웨어 설정 (모든 origin 허용)
- [x] 라우터 모듈 분리 (`routers/`)
- [x] 서비스 레이어 분리 (`services/`)
- [x] 서버 시작 시 캐시 Warm-up (아이템 목록 프리로딩)
- [x] `.env` 환경변수 관리 (DB 접속정보, API 키)

### 1.2 데이터베이스
- [x] SQLite → MySQL 마이그레이션 완료
- [x] MySQL Connection Pool 구성 (`database.py`, pool_size=20)
- [x] DB 초기화 스크립트 작성 (`scripts/init_*.py`)
  - [x] `init_auth_schema.py` - 인증 스키마
  - [x] `init_shop_tables.py` - 쇼핑몰 테이블
  - [x] `init_course_tables.py` - 수강신청 테이블
  - [x] `init_appeal_table.py` - 이의신청 테이블
  - [x] `init_gacha_schema.py` - 가챠 스키마
- [x] DB 디버그/검사 스크립트 (`debug_db.py`, `inspect_*.py`)

### 1.3 사용자 인증 (Authentication)
- [x] JWT 토큰 기반 인증 (`routers/auth.py`)
- [x] 회원가입 (`POST /auth/register`)
- [x] 로그인 (`POST /auth/token`)
- [x] 비밀번호 암호화 (PBKDF2, `core/security.py`)
- [x] 현재 사용자 조회 의존성 (`get_current_user`)
- [x] 관리자 권한 확인 의존성 (`get_admin_user`)

### 1.4 API 라우터
- [x] 인증 라우터 (`routers/auth.py`) - 회원가입, 로그인, 토큰 발급
- [x] 사용자 라우터 (`routers/users.py`) - 멤버 조회
- [x] 수강신청 라우터 (`routers/courses.py`) - 강좌 관련 API
- [x] 이의신청 라우터 (`routers/appeals.py`) - Q&A 게시판
- [x] 쇼핑몰 라우터 (`routers/shop.py`) - 아이템 구매/판매

### 1.5 비즈니스 로직 (Services)
- [x] 사용자 서비스 (`services/user_service.py`)
- [x] 수강신청 서비스 (`services/course_service.py`)
- [x] 이의신청 서비스 (`services/appeal_service.py`)
- [x] 쇼핑몰 서비스 (`services/shop_service.py`)

### 1.6 기타 서버
- [x] MCP 서버 (`server_mcp.py`) - 포트 8002
- [x] API 서버 (`api_server.py`) - 포트 8004
- [x] Gemini Agent 연동 (`agent.py`)
- [x] 서버 일괄 실행 배치파일 (`run_servers.bat`)

---

## 2. 프론트엔드 (React + TypeScript + Vite)

### 2.1 프로젝트 구조
- [x] Vite + React + TypeScript 프로젝트 설정
- [x] 라우팅 구성 (`react-router-dom`)
- [x] 레이아웃 컴포넌트 (`components/layout/`)
- [x] 인증 컨텍스트 (`context/AuthContext.tsx`)
- [x] 보호 라우트 (`ProtectedRoute`, `ManagerRoute`)

### 2.2 페이지
- [x] 로그인 페이지 (`LoginPage.tsx`)
- [x] 홈 페이지 (`HomePage.tsx`)
- [x] 팀 페이지 (`TeamPage.tsx`)
- [x] 날씨 페이지 (`WeatherPage.tsx`)
- [x] 패션 추천 페이지 (`FashionPage.tsx`) — 로그인 필요
- [x] 수강신청 페이지 (`CoursePage.tsx`) — 로그인 필요
- [x] 쇼핑몰 페이지 (`ShopPage.tsx`) — 로그인 필요
- [x] 관리자 페이지 (`ManagerPage.tsx`) — 관리자 전용

### 2.3 API 연동
- [x] 인증 API 서비스 (`services/authApi.ts`)
- [x] 쇼핑몰 API 서비스 (`services/shopApi.ts`)

### 2.4 컴포넌트
- [x] 레이아웃 관련 (`components/layout/` - 3개 파일)
- [x] 쇼핑몰 관련 (`components/shop/` - 4개 파일)
- [x] 패션 관련 (`components/fashion/` - 3개 파일)
- [x] 위젯 (`components/widgets/` - 1개 파일)

### 2.5 기타
- [x] 커스텀 훅 (`hooks/` - 2개 파일)
- [x] 이펙트 (`effects/` - 2개 파일)
- [x] 스타일 (`styles/` - 4개 파일)
- [x] 상수 (`constants/` - 3개 파일)

---

## 3. 권한 관리 (RBAC)

- [x] 백엔드: `role` 필드 (USER/ADMIN) 지원
- [x] 백엔드: `get_admin_user` 의존성 함수
- [x] 프론트: `AuthContext`에서 `role` 관리
- [x] 프론트: `ManagerRoute` 보호 라우트
- [ ] 프론트: `ManagerRoute`에서 실제 role 검증 활성화 (현재 주석 처리)

---

## 4. 문서화

- [x] `README.md` - 프로젝트 개요, 실행 방법, 테스트 계정
- [x] `DB_MANUAL.md` - MySQL 마이그레이션 가이드
- [x] `task.md` - 작업 현황 (본 문서)

---

## 5. 미완료 / 개선 가능 항목

- [ ] `ManagerRoute`의 ADMIN role 검증 주석 해제 및 활성화
- [x] 레거시 코드 정리 (`main.py`의 `/items/{item_id}` 엔드포인트 제거 완료)
- [x] CORS 정책 강화 (특정 origin만 허용 + 환경변수 확장 지원)
- [x] API 에러 핸들링 통일 (글로벌 예외 처리기 추가 완료)
- [ ] 📝 프론트엔드 테스트 코드 추가 (자기 실습)
- [ ] 📝 백엔드 테스트 코드 보강 (자기 실습) → `테스트코드-자기실습용.md` 참고
- [ ] 배포 설정 (Docker, CI/CD 등)
